interface:
  display_name: "Paper Deep Reading"
  short_description: "Evidence-grounded single-paper Chinese reports"
  default_prompt: "Use $paper-deep-reading to create a Chinese view-report.md for this paper, resolve an approved output directory and deterministic task name, stage the main PDF as <task_name>.pdf without changing Zotero, and apply standard validation unless I narrow it."

policy:
  allow_implicit_invocation: true
